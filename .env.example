# API_PROXY_TARGET: 로컬 개발용. website/api는 CORS 헤더를 안 내려주므로,
# Vite dev 서버가 /api 요청을 여기로 프록시해서 브라우저 입장에서 same-origin으로
# 보이게 한다. website를 `vercel dev`로 로컬에 띄웠다면 그 주소를 쓴다.
API_PROXY_TARGET=http://localhost:3000

# 배포 후에는 이 프록시 대신 vercel.json의 rewrite가 같은 역할을 한다 (README 참고).
